import React from "react";

export default function Defaults() {
    return (
        <div>
            <h1>Defaults</h1>
            <p>
                This is the defaults page.
            </p>
        </div>
    );
}